# CreatDAO 测试操作流程文档

---

## 〇、Sepolia 测试网实战指南（2026-06-28）

### 0.1 当前部署信息

| 项目 | 值 |
|------|-----|
| 网络 | Sepolia Testnet（chainId = 11155111） |
| RPC | `https://ethereum-sepolia-rpc.publicnode.com` |
| AuditDAOv2 | `0x90badefFb1d35B0720c32073E6803a72aA41E005` |
| CeatToken | `0x5Ae3d5bDC852D8f88B9ad2dde31bc4721cB6E523` |
| Multisig | `0x431eA6ff1EfBD9F03AC910F15E44EC1FDc10194a` |
| TeamManager | `0xa2823165Bdab1BFB7C9C0560D39380BD0BF855b3` |
| AuditCertificate | `0xD112Ad0e4E287Ce31536a7c5955490F0cD3980d0` |
| 合约要求最低质押 | **500 CEAT**（`minStakeAmount`） |

### 0.2 测试账户配置（10个地址）

| # | 地址 | 角色 | 需要 ETH | 需要 CEAT |
|---|------|------|----------|-----------|
| 1 | `0x147Fcf3EB8B9E305a5b4e16cbba90462F7126db9` | **Owner（管理员）** | ≥ 0.1 | 大量（已有 810,000） |
| 2 | `0xdb03374034ca16Afe997038266886327d1efd3af` | **审计团队** | ≥ 0.01 | ≥ 500 |
| 3 | `0x06157befB8Ca2Ba95dADAF0A1a5d7D13C4f2B523` | **社区用户A** | ≥ 0.01 | ≥ 500 |
| 4 | `0x513c4aF92b8D86786a3C14B51aF8cD08D442B697` | **委员会成员1** | ≥ 0.01 | ≥ 500 |
| 5 | `0x8C0BF74416C5d2fE0025129e274185a2ac9ce144` | **委员会成员2** | ≥ 0.01 | ≥ 500 |
| 6 | `0x8cD480e039415E45ebd2956A6A942D0062105E81` | **委员会成员3** | ≥ 0.01 | ≥ 500 |
| 7 | `0x413Da6f14815929A1dd38EB98179C94dEa824F9B` | **委员会成员4** | ≥ 0.01 | ≥ 500 |
| 8 | `0x32a8152c08EF7eE334Ee8BaB8c4c6F4277d737a7` | **委员会成员5** | ≥ 0.01 | ≥ 500 |
| 9 | `0xE90AFceA4003e84452119546EC9B2Df6beA580C3` | **社区用户B** | ≥ 0.01 | ≥ 500 |
| 10 | `0x20Af5d715E41918e3C3697C70106c1C171484870` | **社区用户C** | ≥ 0.01 | ≥ 500 |

> 委员会和审计团队已通过脚本链上设置完成。每个非 Owner 账户已有 10,000 CEAT。ETH 余额参差不齐——地址 4-10 可能无 ETH，需从水龙头领取或从 Owner 转账。

---

## 一、启动服务

### 1.1 启动后端

```powershell
cd backend
python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

- 访问 `http://localhost:8000/docs` 确认 Swagger 页面加载成功
- 访问 `http://localhost:8000/api/contract/roles` 确认返回正确角色

### 1.2 启动前端

```powershell
cd frontend
python.exe -m http.server 5500
```

- 浏览器打开 `http://localhost:5500`

### 1.3 根目录 .env 确认

```
ADMIN_PRIVATE_KEY=<你的MetaMask钱包私钥>
AUDIT_DAO_ADDRESS=0x90badefFb1d35B0720c32073E6803a72aA41E005
CEAT_TOKEN_ADDRESS=0x5Ae3d5bDC852D8f88B9ad2dde31bc4721cB6E523
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
PORT=8000
DATABASE_URL=sqlite:///audit_dao.db
```

---

## 二、核心测试流程（按正确顺序）

### 提案状态流转图

```
Owner 创建提案 (Submitted)
  → 审计团队提交报告 (TeamReview)
  → Owner 开启社区审核 (CommunityReview)
  → 多位社区用户提交方案
  → Owner 开启投票
  → 社区用户投票
  → Owner 结束投票 (Finalized / CommitteeRuling)
```

> **关键**：`startVoting` 需要同时满足两个条件：
> 1. 提案状态 == `CommunityReview`
> 2. 至少有一个社区方案（`communityProposalHashes.length > 0`）
>
> 另外 `submitCommunityProposal` 要求质押 ≥ 500 CEAT。

---

### 步骤 1：Owner 创建提案

| 方式 | 操作 | MetaMask 账户 |
|------|------|--------------|
| 前端 | 打开 `admin.html`，填入 codeHash → 点击创建 | 账户 1（Owner） |
| 后端 API | `POST /api/admin/create-proposal` | 后端私钥自动签名 |

**参数**：

| 参数名 | 值（示例） | 类型 | 说明 |
|--------|-----------|------|------|
| `code_hash` | `0xa1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2` | hex string | 66 字符（含 0x），32 字节 |

**API 请求**：
```json
POST /api/admin/create-proposal
{ "code_hash": "0xa1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2" }
```

**API 返回**：`{"proposal_id": N}`（N 为链上分配的 ID）

---

### 步骤 2：审计团队提交报告

由于 Sepolia 上审计团队是地址 2，我无法代签（私钥在你的 MetaMask），有三种方式：

| 方式 | 操作 |
|------|------|
| **A. 后端 API**（推荐） | 用 Owner 私钥直接调 API，合约允许 Owner 代替审计团队提交 |
| **B. 前端** | MetaMask 切到账户 2（审计团队）→ 打开 `report-writing.html` |
| **C. Hardhat 脚本** | `npx hardhat run scripts/prep_proposal_and_fund.js --network sepolia`（已写好，一步完成步骤1-3） |

**方式 A — API 请求**：
```json
POST /api/test/submit-report
{
  "proposal_id": N,
  "report_hash": "0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"
}
```
> `report_hash` 必须是 66 字符（含 0x）的十六进制。

---

### 步骤 3：Owner 开启社区审核

**API 请求**：
```json
POST /api/admin/start-community-review
{ "proposal_id": N }
```

**验证**：`GET /api/proposals/N/full-info` → 状态应为 `CommunityReview`

---

### 步骤 4：社区用户提交方案（需要 2-3 个用户各提交一个）

**前置条件**：每个用户必须**质押 ≥ 500 CEAT**（合约 `minStakeAmount` 要求）

| 步骤 | MetaMask 账户 | 前端页面 | 操作 |
|------|-------------|----------|------|
| 4a | **账户 3**（社区用户A） | `dashboard.html` | 连接钱包 → 质押 ≥ 500 CEAT（如已质押可跳过） |
| 4b | 账户 3 | `proposal-writing.html` | 填写表单 → 生成哈希 → 提交方案（提案 ID = N） |
| 4c | 切换到 **账户 9**（社区用户B） | `dashboard.html` | 连接钱包 → 质押 ≥ 500 CEAT |
| 4d | 账户 9 | `proposal-writing.html` | 提交方案（提案 ID = N） |
| 4e | 切换到 **账户 10**（社区用户C） | `dashboard.html` | 连接钱包 → 质押 ≥ 500 CEAT |
| 4f | 账户 10 | `proposal-writing.html` | 提交方案（提案 ID = N） |

**质押参数**（dashboard.html）：

| 参数名 | 页面元素 ID | 最小值 | 类型 |
|--------|-------------|--------|------|
| 质押数量 | `stakeAmount` | `500` | string（CEAT） |

合约调用链：先 `ceatToken.approve(DAO, amount)` 授权 → 再 `auditDAO.stake(amount)` 质押。

**方案提交参数**（proposal-writing.html）：

| 参数名 | 说明 | 类型 |
|--------|------|------|
| 提案 ID | 填入步骤 1 的 `proposal_id` | int |
| 项目名称 | 任意描述 | text |
| 风险等级 | None/Low/Medium/High/Critical | 选项 |
| 测试环境/用例/结论 | 任意填写 | textarea |
| 生成哈希 | 前端基于表单内容自动生成 | — |

**合约调用**：`auditDAO.submitCommunityProposal(proposalId, resultHash)`

> **注意**：
> - 合约允许在 `TeamReview` 或 `CommunityReview` 状态下提交社区方案
> - `proposal-writing.html` 前端额外禁止了 `Arbitration`/`Finalized` 状态提交（status >= 7）
> - 每个账户提交方案需要**不同的哈希值**（前端修改表单内容即可产生不同哈希）

**验证**：任一账户在 `dashboard.html` → "已提交方案" → 输入提案 ID → 应看到多个方案哈希。

---

### 步骤 5：Owner 开启投票

| 方式 | 操作 |
|------|------|
| 后端 API | `POST /api/admin/start-voting` |
| 前端 | MetaMask 切账户 1 → `admin.html` → 控制投票 |

> **注意**：`admin.html` 的"开启投票"按钮会自动执行完整前置流程：`startCommunityReview` → `submitCommunityProposal` → `startVoting`。如果用 API 分开调用，则必须手动完成步骤 3 和步骤 4 再调此接口。

**API 请求**：
```json
POST /api/admin/start-voting
{ "proposal_id": N }
```

**验证**：`GET /api/proposals/N/full-info` → 投票结束时间不为空。

---

### 步骤 6：社区用户投票

| MetaMask 账户 | 前端页面 | 操作 |
|--------------|----------|------|
| 账户 3（社区用户A） | `dashboard.html` → 投票卡片 | 选择提案 ID，输入**想投的方案哈希** |
| 账户 9（社区用户B） | 同上 | 投不同方案或相同方案 |
| 账户 10（社区用户C） | 同上 | 投票 |

**投票参数**（dashboard.html）：

| 参数名 | 页面元素 class | 值（示例） | 类型 | 说明 |
|--------|---------------|-----------|------|------|
| 提案 ID | `.vote-pid` | `N` | int | |
| 方案哈希 | `.vote-hash` | `0x...`（66字符） | string | 从"已提交方案"查询获取 |
| 投票数量 | `.vote-amount` | `0` 或具体数字 | int | 0 = 使用全部质押权重 |

**合约调用**：`auditDAO.vote(proposalId, proposalHash, voteAmount)`

---

### 步骤 7：Owner 结束投票

**API 请求**：
```json
POST /api/admin/finalize-voting
{ "proposal_id": N }
```

---

## 三、完整参数速查表

### 3.1 管理端（admin.html / API）

| 功能 | 接口 | 参数 | 说明 |
|------|------|------|------|
| 创建提案 | `POST /api/admin/create-proposal` | `code_hash`: 66字符 hex | 32 字节代码哈希 |
| 开启社区审核 | `POST /api/admin/start-community-review` | `proposal_id`: int | 前置：已提交团队报告 |
| 开启投票 | `POST /api/admin/start-voting` | `proposal_id`: int | 前置：状态=CommunityReview 且有社区方案 |
| 结束投票 | `POST /api/admin/finalize-voting` | `proposal_id`: int | |
| 分配奖励 | `POST /api/admin/allocate-rewards` | `recipient`: 地址, `amount`: Wei 字符串 | |
| 罚没质押 | `POST /api/admin/slash-stake` | `staker`: 地址, `amount`: Wei 字符串 | |
| 投票奖惩 | `POST /api/admin/apply-vote-rewards` | `proposal_id`: int, `final_hash`: 66字符 hex | |
| 转移所有权 | `POST /api/admin/transfer-ownership` | `new_owner`: 地址 | |
| 查询角色 | `GET /api/contract/roles` | 无 | 返回 owner/audit_team/committee |
| 查询提案详情 | `GET /api/proposals/N/full-info` | path: N | 链上+数据库完整信息 |
| 提案列表 | `GET /api/proposals?skip=0&limit=100` | query: skip, limit | |

### 3.2 社区用户端（dashboard.html）

| 功能 | 页面元素 | 参数 | 合约调用 |
|------|----------|------|----------|
| 购买 CEAT | `ethAmount` | ETH 数量（1 ETH = 100 CEAT） | `ceatToken.buyCEAT()` 附 value |
| 质押 | `stakeAmount` | ≥ 500 CEAT | 先 `approve` 再 `stake` |
| 解除质押 | `unstakeAmount` | 数量 CEAT | `auditDAO.unstake(amount)` |
| 提交社区方案 | proposal-writing.html | proposal_id + 表单 | `auditDAO.submitCommunityProposal()` |
| 投票 | `.vote-pid` `.vote-hash` `.vote-amount` | 提案ID + 方案哈希 + 票数(0=全部) | `auditDAO.vote()` |
| 查询方案 | `queryProposalId` | 提案ID | `getCommunityProposalHashes()` |
| 查询余额 | 质押卡片 → 获取余额 | — | `balanceOf` + `getStakerInfo` |

### 3.3 审计团队端（audit-team.html / report-writing.html）

| 功能 | 参数 | 合约调用 | 备注 |
|------|------|----------|------|
| 接取提案 | `proposal_id`: int | 后端仅更新数据库状态 | 不调用链上合约 |
| 提交审计报告 | `proposal_id` + `report_hash`(66字符) | `auditDAO.submitTeamReport()` | |
| 发起否决 | `proposal_id` + `evidence`(string) | `auditDAO.initiateVeto()` | |

### 3.4 争议委员会端（dispute-committee.html）

| 功能 | 参数 | 合约调用 |
|------|------|----------|
| 委员会投票 | `proposal_id` + `support_auditor`(bool) | `auditDAO.committeeVote()` |

### 3.5 定价与订单（pricing.html / API）

| 参数 | 可选值 | 说明 |
|------|--------|------|
| `tier` | `standard` / `deep` | 服务档位 |
| `complexity` | `simple` / `moderate` / `complex` / `highly_complex` | 复杂度 |
| `asset_pool_size` | `micro` / `small` / `medium` / `large` / `extra_large` | 资产池规模 |
| `code_lines` | number | 代码行数 |
| `contract_count` | number | 合约数量 |
| `need_bounty` | bool | 是否添加赏金 |
| `bounty_amount` | number (USD) | 赏金金额 |
| `has_dispute` | bool | 是否含争议 |
| `pay_with_token` | bool | 用代币支付 |

---

## 四、合约调用参考

### 4.1 权限要求

| 合约函数 | 谁可以调用 |
|----------|-----------|
| `createProposal` | 仅 Owner |
| `startVoting` | 仅 Owner |
| `startCommunityReview` | 仅 Owner |
| `finalizeVoting` | Owner 随时 / 任何人但需投票已过期 |
| `submitTeamReport` | Owner 或 auditTeam |
| `submitCommunityProposal` | 任何人（质押 ≥ 500 CEAT） |
| `vote` | 任何人（质押 ≥ 500 CEAT，未重复投票） |
| `stake` | 任何人 |
| `committeeVote` | 仅委员会成员 |
| `setCommitteeMembers` | 仅 Owner |
| `setAuditTeam` | 仅 Owner |
| `allocateRewards` / `slashStake` / `applyVoteRewardsPenalties` | 仅 Owner |
| `transferOwnership` | 仅 Owner |

### 4.2 查询函数（任何人可调用）

| 函数 | 参数 | 返回 |
|------|------|------|
| `getProposalSummary(id)` | proposalId | id, codeHash, auditReportHash, winningCommunityHash, secondReviewHash, status, riskLevel, createdAt, disputeCreatedAt, votingEndTime... |
| `getStakerInfo(address)` | 地址 | balance, rewards, contributionPoints, reputationScore |
| `getCommunityProposalHashes(id)` | proposalId | bytes32[] |
| `getCommunityProposalVotes(id, hash)` | proposalId, hash | uint256 |
| `getCommitteeMembers()` | 无 | address[] |
| `minStakeAmount()` | 无 | 500 * 10^18 (500 CEAT) |

---

## 五、常见问题与坑

### 5.1 开启投票失败（revert）

**原因**：不满足两个条件：
1. 状态不是 `CommunityReview` → 先调 `start-community-review`
2. 没有社区方案 → 让社区用户先 `submitCommunityProposal`

### 5.2 提交社区方案失败（API 返回 success 但链上无方案）

**原因**：质押不足 500 CEAT，合约 revert 但 API 未检测（**已修复**，现在会校验 receipt.status）。

### 5.3 投票失败

**原因**：质押量 < 500 CEAT，或已投过票。

### 5.4 前端连接不上后端

检查 `frontend/config.js`：
```javascript
SEPOLIA_CHAIN_ID_DEC: 11155111,  // 不是 31337！
SEPOLIA_RPC: 'https://ethereum-sepolia-rpc.publicnode.com',
```

### 5.5 MetaMask 不弹窗

1. 确认选择 Sepolia 测试网络
2. 确认当前账户有 ETH 付 Gas
3. 确认 `config.js` chainId 是 `11155111`
4. 检查 MetaMask 设置 → 高级 → 重置账户（清除 nonce 缓存）

### 5.6 后端启动报端口占用

```powershell
netstat -ano | findstr :8000
# 找到 PID 后
Stop-Process -Id <PID> -Force
```

---

## 六、一键脚本参考

### 快速准备（创建提案 + 提交团队报告 + 开社区审核 + 给社区用户转 ETH）

```bash
npx hardhat run scripts/prep_proposal_and_fund.js --network sepolia
```

### 批量设置账户（委员会、审计团队、CEAT 转账）

```bash
npx hardhat run scripts/setup_sepolia_accounts.js --network sepolia
```

---

## 七、Hardhat 本地模式参考（31337）

仅当需要在本地快速测试时使用。以下为关键差异：

| 对比项 | Hardhat 本地 | Sepolia |
|--------|-------------|---------|
| chainId | `31337` | `11155111` |
| 启动命令 | `npx hardhat node` | 不需要 |
| 部署命令 | `--network localhost` | `--network sepolia` |
| `.env` RPC | `http://127.0.0.1:8545` | `https://ethereum-sepolia-rpc.publicnode.com` |
| `.env` 私钥 | `0xac0974...` (Hardhat #0) | 你的真实私钥 |
| `config.js` chainId | `31337` | `11155111` |
| `config.js` RPC | `http://127.0.0.1:8545` | Sepolia RPC |
| MetaMask 网络 | 手动添加 | 内置 |
| 合约地址 | 每次部署相同 | 每次部署不同 |

### 本地部署步骤

```bash
npx hardhat node                                              # 终端1
npx hardhat run scripts/deploy_token.js --network localhost   # 终端2
npx hardhat run scripts/deploy_v3.js --network localhost
npx hardhat run scripts/deploy_v3_finish.js --network localhost
```

然后将部署输出的合约地址填入 `.env` 和 `frontend/config.js`。

### Hardhat 默认账户（chainId 31337）

| # | 角色 | 地址 |
|---|------|------|
| 0 | Owner | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` |
| 1 | 审计团队1 | `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` |
| 2 | 审计团队2 | `0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC` |
| 3-5 | 委员会 | `0x90F79...` / `0x15d34...` / `0x99655...` |
| 6-9 | 社区用户 | `0x976EA...` / `0x14dC7...` / `0x23618...` / `0xa0Ee7...` |

私钥见 Hardhat 文档或 `npx hardhat node` 输出。
