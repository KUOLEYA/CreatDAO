# CreatDAO 测试操作流程

## 一、页面导航概览

| 页面 | URL | 用途 | 入口 |
|------|-----|------|------|
| 主页 | `home.html` | 平台入口 | 直接访问 |
| 提交审计 | `ai-review.html` | AI 审计 + 支付 + 创建链上提案 | 主页 → "提交审计" |
| 定价 | `pricing.html` | 查看服务层级 | 主页 → "定价" |
| 用户端 | `dashboard.html` | 社区提案列表、质押、投票 | 主页 → "社区治理" |
| 管理员 | `admin.html` | 管理员面板 | 主页 → "管理" |
| 审计团队 | `audit-team.html` | 审计团队工作台 | 主页 → "审计团队" |
| 团队报告编写 | `report-writing.html` | 审计团队提交正式报告 | AI审计成功页 / audit-team |
| 社区报告编写 | `community-report-writing.html` | 社区用户提交审计方案 | dashboard → 领取后跳转 |
| 争议委员会 | `dispute-committee.html` | 委员会投票仲裁 | 主页 / dashboard |

> **钱包连接状态**：所有页面共用同一个钱包状态。在一个页面连接钱包后，切换页面会自动保持连接。右上角会显示 `🟢 0xf39F...2266` 表示已连接。

---

## 二、网络选择

系统支持 **双网络**：Hardhat 本地测试网 和 Sepolia 测试网，前端自动检测 MetaMask 当前网络并加载对应的合约地址。

| 网络 | chainId | 优点 | 推荐 |
|------|---------|------|------|
| Hardhat 本地 | 31337 | 10000 ETH，无需 faucet | ✅ |
| Sepolia | 11155111 | 真实测试网 | - |

> **网络切换**：在 `dashboard.html` 右上角点击网络标识，下拉选择即可一键切换。

---

## 三、Hardhat 本地测试（推荐）

### 3.1 环境信息

| 项目 | 值 |
|------|-----|
| 网络 | Hardhat Local，chainId 31337 |
| RPC | `http://127.0.0.1:8545` |
| 后端 | `http://localhost:8000` |
| 前端 | `http://127.0.0.1:5500/home.html` |

### 合约地址

```
AuditDAOv2     : 0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9
CeatToken      : 0x5FbDB2315678afecb367f032d93F642f64180aa3
AuditTeamManager: 0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0
DepositProof   : 0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
Hello          : 0xc6e7DF5E7b4f2A278906862b61205850344D4e7d  (用于AI审计测试)
```

### MetaMask 配置

添加自定义网络：**chainId `31337`**，RPC `http://127.0.0.1:8545`

### 10 个测试账户

| # | 私钥（导入 MetaMask 用） | 角色 |
|---|-------------------------|------|
| 1 | `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80` | Owner |
| 2 | `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d` | 审计团队 |
| 3 | `0x5de4111afa1a4b94908f83103eb1f1706367c2e68ca870fc3fb9a804cdab365a` | 社区用户A |
| 4 | `0x7c852118294e51e653712a81e05800f419141751be58f605c371e15141b007a6` | 社区用户B |
| 5 | `0x47e179ec197488593b187f80a00eb0da91f1b9d0b13f8733639f19c30a34926a` | 委员会1 |
| 6 | `0x8b3a350cf5c34c9194ca85829a2df0ec3153be0318b5e2d3348e872092edffba` | 委员会2 |
| 7 | `0x92db14e403b83dfe3df233f83dfa3a0d7096f21ca9b0d6d6b8d88b2b4ec1564e` | 委员会3 |
| 8 | `0x4bbbf85ce3377467afe5d46f804f221813b2bb87f24d81f60f1fbcdab7d44ed1` | 委员会4 |
| 9 | `0xdbda1821b80551c9d65939329250298aa3472ba22feea921c0ade023b8a2aac0` | 委员会5 |
| 10 | `0x2a871d0798f97d79848a013d4936a73bf4cc922c825d33c1cf7073dff7b7a71a` | 社区用户C |

地址详情：
| # | 地址 | CEAT | ETH |
|---|------|------|-----|
| 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | 1,000,000 | 10000 |
| 2 | `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` | 100,000 | 10000 |
| 3 | `0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC` | 100,000 | 10000 |
| 4 | `0x90F79bf6EB2c4f870365E785982E1f101E93b906` | 100,000 | 10000 |
| 5 | `0x15d34AAf54267DB7D7c367839AAf71A00a2C6A65` | 100,000 | 10000 |
| 6 | `0x9965507D1a55bcC2695C58ba16FB37d819B0A4dc` | 100,000 | 10000 |
| 7 | `0x976EA74026E726554dB657fA54763abd0C3a0aa9` | 100,000 | 10000 |
| 8 | `0x14dC79964da2C08b23698B3D3cc7Ca32193d9955` | 100,000 | 10000 |
| 9 | `0x23618e81E3f5cdF7f54C3d65f7FBc0aBf5B21E8f` | 100,000 | 10000 |
| 10 | `0xa0Ee7A142d267C1f36714E4a8F75612F20a79720` | 100,000 | 10000 |

> **重新测试**：需重新部署+重启后端。Hardhat 重启后 MetaMask 需**重置账户**（设置 → 高级 → 重置账户）以清除非ce和交易历史。

### 3.2 启动服务与部署（4个终端）

```
# 终端 A — Hardhat 节点
npx hardhat node --hostname 127.0.0.1 --port 8545

# 终端 B — 部署合约（节点启动后执行一次）
npx hardhat run scripts/local_deploy.js --network localhost

# 终端 C — 后端（部署后需重启以读取新 .env）
cd backend; python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 终端 D — 前端
cd frontend; python.exe -m http.server 5500 --bind 127.0.0.1
```

#### `local_deploy.js` 自动完成以下初始化：

| 操作 | 说明 |
|------|------|
| 部署 5 个合约 | CeatToken、DepositProof、AuditTeamManager、AuditDAOv2、Hello |
| 设置合约关联 | DepositProof → AuditDAO、AuditTeamManager → AuditDAO |
| 指定审计团队 | 地址 2 (`0x7099...`) 设为审计团队 |
| 注册审计团队到 Manager | 注册 "CreatDAO Core Audit Team"（teamId=0，成员：地址2+地址3） |
| 指定 5 名委员会成员 | 地址 5–9 共 5 人 |
| 分发 CEAT 代币 | 地址 2–10 各 **100,000 CEAT**，AuditDAO 奖励池 **100,000 CEAT** |
| 设置提案创建费 | 每次创建提案需 **100 CEAT** |
| 生成 `.env` 文件 | 自动写入 `LOCAL_AUDIT_DAO_ADDRESS`、`LOCAL_CEAT_TOKEN_ADDRESS` 等，同时保留已有的 `SEPOLIA_*` 配置 |

> **重要**：部署后必须**重启后端**（终端 C），因为 `.env` 被更新了，后端需要重新读取。

---

### 3.3 完整测试步骤

##### 流程总览

```
§3.3.1 AI审计+创建提案（地址10）
  → §3.3.2 审计团队提交报告（地址2）
  → §3.3.3 社区质押+提交方案（地址3/4/10）
  → §3.3.4 开启投票（地址1）
  → §3.3.5 投票（地址3/4/10）
  → §3.3.6 结束投票（地址1）
  → §3.3.7 结束讨论（地址1，需快进2天）
        └─ 争议（常态）→ §3.3.8 争议解决（3步）
                              └─ §3.3.9 领取奖励
```

| 章节 | 内容 | 操作方 |
|------|------|--------|
| §3.3.1 | AI 审计与链上提案创建 | 地址10 |
| §3.3.2 | 审计团队提交正式报告 | 地址2 |
| §3.3.3 | 社区质押、领取提案、提交方案 | 地址3/4/10 |
| §3.3.4 | 开启投票 | 地址1 |
| §3.3.5 | 投票 | 地址3/4/10 |
| §3.3.6 | 结束投票 | 地址1 |
| §3.3.7 | 结束讨论 & 奖励结算（快进时间） | 地址1 |
| §3.3.8 | 争议解决（3步，仅争议时） | 地址3-10 + 地址1 + 地址5-9 |
| §3.3.9 | 领取奖励 | 地址3/4/10 |

---

#### 3.3.1 AI 审计与链上提案创建（一步完成）

> **页面**：提交审计 (`ai-review.html`)  
> **MetaMask**：地址 10 (社区用户C)

这是核心流程，在一页内完成：**上传代码 → AI 审计 → 支付 → 生成链上提案 → 跳转编写报告**。

**步骤1：配置审计费用**
1. 预估代码行数：`1000`
2. 合约复杂度：选择 `中等 ×1.0`
3. 资产池规模：选择 `小型`
4. 附加赏金池：不勾选

**步骤2：上传合约代码**
1. 拖拽 `hello.sol` 到上传区域
2. 填入以下参数（可直接复制）：

合约地址：0xc6e7DF5E7b4f2A278906862b61205850344D4e7d
项目名称：HelloWorld_Test
业务逻辑：Hello 是一个简单的演示合约，用于存储和读取一个字符串消息，无代币功能：set() 设置消息，get() 读取消息。
审计范围：Hello 合约的所有函数：set() 写入功能、get() 读取功能
已知风险：无已知风险
联系方式：test@creatdao.io
期望完成日期：2026-07-16
最大天数：7
附加任务奖励：不勾选
```

3. 点击 **提交 AI 审计**，等待 AI 分析完成

**步骤3：支付与创建链上提案**
1. 在 AI 审计结果页，点击 **💎 使用 CEAT 代币支付**
2. MetaMask 确认交易（自动检测网络，验证余额）
3. 支付成功后，点击 **🚀 生成链上提案**
4. MetaMask 再次确认交易
5. 看到 `✅ 提案创建成功！`，记录**提案 ID**

**步骤4：跳转编写报告**
- 成功页会显示 4 个按钮，第一个是 **📝 编写并提交提案报告**
- 点击后跳转到 `report-writing.html`，提案 ID、合约地址、代码哈希自动填入

#### 3.3.2 审计团队提交报告

> **页面**：审计团队报告编写 (`report-writing.html`)  
> **MetaMask**：地址 2 (审计团队)
> 
> 如果从 AI 审计成功页跳转，**提案 ID、合约地址、代码哈希已自动填入**。

1. 点击 **📝 填写样例** 自动填充全部内容（风险评级自动设为低危，发现矩阵、分歧点等一并填入）
2. 确认风险评级已选中（� 低危应高亮显示），如未选中请手动点击
3. 确认以下信息正确：

```
提案 ID：（已预填，与 AI 审计创建的提案 ID 一致，0 是合法的）
合约地址：0xc6e7DF5E7b4f2A278906862b61205850344D4e7d
代码哈希：自动从 URL 参数填入
```

4. 依次点击 **生成报告** → **预览报告** → **提交报告**
5. MetaMask 确认交易

#### 3.3.3 社区质押与提交方案

> 社区用户在团队报告提交后，即可在 dashboard.html 上质押并提交自己的审计方案。  
> Owner 稍后在 admin.html 上执行 **开启投票**（一键完成社区审核开启 + 投票启动）。

##### ① 地址 3（社区用户A）

> **MetaMask**：地址 3

**步骤 1：质押 CEAT**
- 页面：用户端 (`dashboard.html`)
- 输入质押数量：`500`
- 点击 **授权并质押**
- MetaMask 确认 2 笔交易（approve + stake）

**步骤 2：领取提案**
- 页面：用户端 (`dashboard.html`)
- 在「领取提案并开始审核」卡片中，点击 **刷新可领取任务列表**
- 找到对应提案，点击 **领取提案**
- MetaMask 确认

**步骤 3：提交社区方案**
- 领取成功后，点击出现的 **社区审计报告编写** → 跳转至 `community-report-writing.html?proposal_id=0`
- **代码哈希和合约地址会自动加载**（页面初始化时调用了 `loadProposalInfo()`）
- 点击 **📝 填写样例** 自动填充全部内容（共识投票证明、贡献者名单、对齐意见书、签名等）
- 点击 **生成报告** → **预览报告** → **提交至链上**
- MetaMask 确认

##### ② 地址 4（社区用户B）

> **MetaMask**：地址 4

重复同样步骤：
1. 质押 `500` CEAT
2. 领取提案
3. 提交社区方案（内容可随意修改，或用同样模板）

##### ③ 地址 10（社区用户C）

> **MetaMask**：地址 10

重复同样步骤：
1. 质押 `500` CEAT
2. 领取提案
3. 提交社区方案

#### 3.3.4 开启投票

> **页面**：管理员端 (`admin.html`)  
> **MetaMask**：地址 1 (Owner)  
> 
> 点击"开启投票"会**自动依次执行**：开启社区审核 → 提交社区方案 → 启动投票。

1. 填写提案 ID
2. 设置开始时间（点击 **设为现在**）
3. 设置结束时间（点击 **设置为7天后**）
4. 点击 **开启投票**
5. MetaMask 确认交易

#### 3.3.5 投票阶段

> **MetaMask**：依次切换到地址 3 → 4 → 10

**投票要求**：
- 地址 3：投自己的方案
- 地址 4：投自己的方案
- 地址 10：投地址 3 的方案（制造票差）

**操作**：
- 页面：用户端 (`dashboard.html`)
- 在「投票」卡片中，点击 **+ 添加提案**
- 填入提案 ID 和方案哈希
- 投票数量：填 `0`（使用全部质押权重）
- 点击 **一键投票**

#### 3.3.6 结束投票

> **页面**：管理员端 (`admin.html`)  
> **MetaMask**：地址 1 (Owner)

1. 填写提案 ID
2. 点击 **提前结束投票**
3. MetaMask 确认交易

#### 3.3.7 结束讨论 & 奖励结算

> **页面**：管理员端 (`admin.html`)  
> **MetaMask**：地址 1 (Owner)  
> **页面位置**：「控制投票」卡片最下方，**结束讨论** 按钮紧挨着"提前结束投票"

##### 背景说明

投票结束后，提案进入 **讨论期（Discussion）**，合约设定讨论期为 **2 天**。在这 2 天内，社区成员可以继续争论提案内容。讨论期满后，管理员调用 `finalizeDiscussion` 来触发最终裁决：

- **共识路径**：`社区投票胜出方案 == 审计团队报告` → 提案 Finalized，自动发放投票奖励 + 社区贡献奖励
- **争议路径**：`社区投票胜出方案 ≠ 审计团队报告` → 提案进入 FirstDispute（争议状态），**不发放奖励**，需委员会仲裁

##### 步骤 0：快进时间（Hardhat 本地必须做）

> 合约讨论期 2 天，本地测试必须快进时间。**另开一个终端**执行：

```bash
npx hardhat run scripts/advance_time.js --network localhost
```

看到 `✅ 时间已快进 2 天` 即成功。

##### 步骤 1：执行结束讨论

1. 硬刷新 admin.html（`Ctrl+Shift+R`，确认为最新版）
2. 在 **提案 ID** 输入框填写提案 ID
3. 点击 **结束讨论**
4. MetaMask 确认交易
5. 观察结果提示：
   -  `社区与审计团队意见不一致，已进入争议阶段（无奖励发放）` → 继续 §3.3.8 争议解决（**正常流程，几乎必然**）
   - 🟢 `达成共识，奖励已自动结算` → 跳到 §3.3.9 领取奖励（极罕见，需社区哈希=审计哈希）

##### 步骤 2：手动检查状态（可选，控制台 F12）

如果前端提示不明确，可打开浏览器控制台（F12）输入：

```javascript
const p = await _A().getProposalSummary(0);
console.log('状态:', p.status, '审计:', p.auditReportHash, '社区:', p.winningCommunityHash);
```

| status 值 | 含义 |
|-----------|------|
| 8 | Finalized — 共识达成，奖励已结算 |
| 4 | FirstDispute — 进入争议，**无奖励** |

#### 3.3.8 争议解决（3步流程）

> 当结束讨论后社区投票结果与审计团队报告不一致时，提案进入争议流程。争议解决共分 3 步。解决后奖励自动结算，回到 §3.3.9 领取。

##### 背景说明

```
FirstDispute (4) → SecondReview (5) → CommitteeRuling (6) → Finalized (8)
```

| 步骤 | 状态 | 操作方 | 操作 | 页面 |
|------|------|--------|------|------|
| ① | FirstDispute | 社区用户 | 投票是否认可审计团队报告 | dashboard「争议投票」 |
| ① | FirstDispute | Owner | 裁决第1步结果 | admin「争议管理」 |
| ② | SecondReview | 审计团队(地址2) | 提交修订报告 | report-writing |
| ② | SecondReview | 社区用户 | 投票是否认可二次复核 | dashboard「争议投票」 |
| ② | SecondReview | Owner | 裁决二次复核结果 | admin「争议管理」 |
| ③ | CommitteeRuling | 委员会(地址5-9) | 5人投票，3/5通过 | dispute-committee |

---

##### 步骤 ①：社区投票 + Owner 裁决（FirstDispute）

###### ①-A：社区用户投票

> **页面**：用户端 (`dashboard.html`)  
> **位置**：滚动到「⚖️ 争议投票」卡片  
> **MetaMask**：地址 3、4、10（有质押的社区用户）

1. 填入提案 ID
2. 点击 **❌ 拒绝审计团队报告（第1步）**（或 ✅ 认可，根据测试需要）
3. MetaMask 确认交易

> 每个地址只能投一次。投票权重 = 质押余额。

###### ①-B：Owner 裁决

> **页面**：管理员端 (`admin.html`)  
> **位置**：滚动到「⚖️ 争议管理」区域  
> **MetaMask**：地址 1 (Owner)

1. 先点 **📋 查看争议状态** 确认当前状态为 `FirstDispute`
2. 填入提案 ID
3. 点击 **① 裁决争议第1步**
4. MetaMask 确认交易
5. 观察结果：
   - 🟢 社区接受 → 提案终结，奖励结算
   - 🟡 社区拒绝 → 进入步骤②

---

##### 步骤 ②：审计团队修订 + 社区投票 + Owner 裁决（SecondReview）

> 仅当步骤①中社区拒绝审计团队报告时，才会进入此步骤。

###### ②-A：审计团队提交修订报告

> **页面**：审计团队报告编写 (`report-writing.html`)  
> **MetaMask**：地址 2 (审计团队)

1. 页面自动检测 `SecondReview` 状态的提案，显示「🔄 修订报告」卡片
2. 确认提案 ID 正确，点击 **📥 加载原报告内容**
3. 在上方表单中修改报告内容（或重新「填写样例」后生成报告）
4. 点击 **提交修订报告**
5. MetaMask 确认交易

###### ②-B：社区用户投票

> **页面**：用户端 (`dashboard.html`)  
> **位置**：「⚖️ 争议投票」卡片第2步按钮  
> **MetaMask**：地址 3、4、10

1. 填入提案 ID
2. 点击 **❌ 拒绝二次复核报告（第2步）**（或 ✅ 认可）
3. MetaMask 确认交易

###### ②-C：Owner 裁决

> **页面**：管理员端 (`admin.html`)  
> **MetaMask**：地址 1 (Owner)

1. 点击 **② 裁决二次复核**
2. MetaMask 确认交易
3. 观察结果：
   - 🟢 二次复核通过 → 提案终结
   - 🟡 二次复核未通过 → 进入步骤③（委员会裁决）

---

##### 步骤 ③：争议委员会裁决（CommitteeRuling）

> 仅当步骤②中二次复核被拒绝时，才会进入此步骤。

###### ③-0：委员会成员质押（必须！）

> 委员会成员必须先质押 CEAT 才有投票权重。质押余额 = 投票权重。

1. **地址 5-9** 切换到用户端 (`dashboard.html`)
2. 各输入质押数量：`500`
3. 点击 **授权并质押**
4. MetaMask 确认 2 笔交易（approve + stake）

###### ③-1：委员会投票

> **页面**：争议委员会 (`dispute-committee.html`)  
> **MetaMask**：地址 5-9（委员会1-5，需先完成质押）

1. **硬刷新** dispute-committee.html（`Ctrl+Shift+R`）
2. 填入提案 ID
3. 选择投票方向：
   - **支持审计方** = 最终采纳审计团队报告
   - **支持社区方** = 最终采纳社区胜出方案
4. 点击 **投票**
5. MetaMask 确认交易
6. 当任一方获得 **3 票**时，合约自动终结提案并分配奖励

> 5 名委员依次投票。任一方先达到 3 票即触发终结，后续委员无法再投。

##### 争议状态速查表

> 先用 admin「📋 查看争议状态」确认当前状态，再按对应步骤操作。

| status 值 | 名称 | 含义 |
|-----------|------|------|
| 4 | FirstDispute | 步骤①：社区投票认可审计团队 |
| 5 | SecondReview | 步骤②：审计团队二次复核 |
| 6 | CommitteeRuling | 步骤③：委员会裁决 |

> **注意**：Hardhat 本地节点状态会跨会话持久化。如果之前已执行过某些步骤，直接用「查看争议状态」确认当前阶段，跳过已完成步骤。

---

#### 3.3.9 领取奖励

> **页面**：用户端 (`dashboard.html`)  
> **MetaMask**：投票过的地址（3、4、10 等）

结束讨论达成共识或争议解决后，合约自动将奖励分配到各投票者的 `pendingRewards`。

1. 切换到投票过的账户
2. 在「领取奖励」卡片查看待领取奖励
3. 点击 **领取奖励**
4. MetaMask 确认交易

> **注意**：如果结束讨论走了争议路径，需要先完成 §3.3.8 争议解决的 3 步流程才能领到奖励。

---

## 四、Sepolia 测试网部署（可选）

### 4.1 前置准备

1. **获取 Sepolia ETH**
   - Sepolia Faucet：https://sepoliafaucet.com
   - 或 Infura Faucet：https://www.infura.io/faucet/sepolia

2. **配置 `.env`**
   - 复制 `.env.example` → `.env`（如果已有本地配置则直接编辑）
   - 填入 Sepolia 合约地址和私钥：
     ```
     # 切换到 Sepolia 活跃网络
     CHAIN_ID=11155111

     # Sepolia 合约地址（部署后填写）
     SEPOLIA_AUDIT_DAO_ADDRESS=0x...
     SEPOLIA_CEAT_TOKEN_ADDRESS=0x...
     SEPOLIA_TEAM_MANAGER_ADDRESS=0x...

     # 管理员私钥（不带0x前缀，用于后端发链上交易）
     ADMIN_PRIVATE_KEY=你的私钥

     # Sepolia RPC
     SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

     # AI 审计
     DEEPSEEK_API_KEY=你的API密钥
     ```

3. **MetaMask 配置**
   - 网络：Sepolia，chainId `11155111`
   - RPC：`https://ethereum-sepolia-rpc.publicnode.com`

4. **重启后端**（读取新的 `.env`）

### 4.2 部署合约

```
npx hardhat run scripts/deploy_sepolia.js --network sepolia
```

部署完成后，将输出的合约地址填入 `.env` 的 `SEPOLIA_AUDIT_DAO_ADDRESS`、`SEPOLIA_CEAT_TOKEN_ADDRESS`、`SEPOLIA_TEAM_MANAGER_ADDRESS`，然后重启后端。

### 4.3 使用流程

与 Hardhat 本地测试相同。前端会自动检测 MetaMask 当前网络并加载对应的合约地址。

**网络切换**：在 dashboard 页面点击右上角网络标识（橙色 "Sepolia" / 绿色 "Local"），下拉选择目标网络即可一键切换。

---

## 五、一键脚本

```
# 本地自动化全流程（AI审计 → DAO提案 → 投票 → 结束）
npx hardhat run scripts/local_full_test.js --network localhost

# 本地重新部署
npx hardhat run scripts/local_deploy.js --network localhost

# 部署至 Sepolia
npx hardhat run scripts/deploy_sepolia.js --network sepolia
```

---

## 六、测试清单

| # | 步骤 | 页面 | 钱包 | ☐ |
|---|------|------|------|---|
| 0 | 启动服务 + 部署合约 | 终端 | — | ☐ |
| 1 | AI 审计 + 支付 + 创建提案 | ai-review.html | 地址10 | ☐ |
| 2 | 跳转编写报告（自动填入） | 点击按钮 | — | ☐ |
| 3 | 团队提交正式报告 | report-writing.html | 地址2 | ☐ |
| 4 | 地址3质押+领取+提交方案 | dashboard → community-report-writing | 地址3 | ☐ |
| 5 | 地址4质押+领取+提交方案 | dashboard → community-report-writing | 地址4 | ☐ |
| 6 | 地址10质押+领取+提交方案 | dashboard → community-report-writing | 地址10 | ☐ |
| 7 | 开启投票 | admin.html | 地址1 | ☐ |
| 8 | 地址3投票 | dashboard.html | 地址3 | ☐ |
| 9 | 地址4投票 | dashboard.html | 地址4 | ☐ |
| 10 | 地址10投票 | dashboard.html | 地址10 | ☐ |
| 11 | 结束投票 | admin.html | 地址1 | ☐ |
| 12 | 快进时间（本地测试） | 终端 | — | ☐ |
| 13 | 结束讨论 & 奖励结算 | admin.html | 地址1 | ☐ |
| 14 | 争议①-A：社区投票（认可/拒绝） | dashboard「争议投票」 | 地址3/4/10 | ☐ |
| 15 | 争议①-B：Owner 裁决第1步 | admin「争议管理」 | 地址1 | ☐ |
| 16 | 争议②-A：审计团队提交修订报告 | report-writing / F12 | 地址2 | ☐ |
| 17 | 争议②-B：社区投票（认可/拒绝修订） | dashboard「争议投票」 | 地址3/4/10 | ☐ |
| 18 | 争议②-C：Owner 裁决二次复核 | admin「争议管理」 | 地址1 | ☐ |
| 19 | 争议③-0：委员会成员质押 | dashboard.html | 地址5-9 | ☐ |
| 20 | 争议③-1：委员会投票（3/5通过） | dispute-committee.html | 地址5-9 | ☐ |
| 21 | 领取奖励 | dashboard.html | 地址3/4/10 | ☐ |
