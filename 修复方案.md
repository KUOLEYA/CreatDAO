# 审计系统缺陷修复方案

## 1. 提案内容可见性问题（Q1、Q5）
**问题描述**：投票时只能看到哈希，看不到方案内容
**修复方案**：新增提案查询功能，支持通过输入提案哈希检索对应方案内容，审计团队与社区成员均可使用该功能。

## 2. 共识达成机制优化（Q2）
**问题描述**：全文哈希几乎不可能一致，导致共识路径形同虚设，争议必然发生、共识几乎无法达成
**修复方案**：
- 降低共识粒度，不再以完整审计报告作为共识对象，将共识维度下沉至**漏洞判定层**。
- 审计团队和社区分别生成统一格式的漏洞判定矩阵，包含漏洞ID、有效性、风险等级、修复状态等结构化字段，链上仅比对矩阵哈希是否一致。
- 全部漏洞判定一致即可直接达成共识并进入最终状态；报告正文、PoC、修复建议等描述性内容仅作为链下附件保存，不参与共识判断。
- 若矩阵中仅个别漏洞存在分歧，仅针对分歧漏洞进入后续讨论或仲裁流程，避免因文档格式、措辞差异人为制造争议，让共识路径具备实际可达性。

## 3. 分歧流程衔接优化（Q4）
**问题描述**：争议流程各步骤依赖不同角色在不同页面手动衔接，缺少流程通知，容易中断
**修复方案**：
- 在每个关键状态流转后建立统一的任务通知机制：合约触发对应事件（Event），后端监听事件后自动生成下一阶段待办任务，并向对应角色推送站内通知或页面提示。
- 典型场景示例：
  - 第一轮分歧裁决结束进入二次复核阶段时，系统自动通知当前审计团队提交修订报告；
  - 进入委员会仲裁阶段时，自动通知具备仲裁资格的委员参与投票，并在 Dashboard 中展示待处理事项、截止时间和当前流程状态。
- 将人工查阅驱动的串行流程，转变为系统主动引导的事件驱动流程，避免角色切换、信息不同步导致的流程中断，提升运行效率与测试体验。

## 4. 委员会机制调整（Q6、Q12）
**问题描述**：委员会成员需要为单次任务临时质押，但绝大多数场景下不会真正参与仲裁，参与成本高
**修复方案**：
- 取消任务级临时质押机制，改为**长期席位质押制度**：委员会成员一次性完成全局席位质押即可获得仲裁资格，系统通过随机或轮换方式选取实际参与案件的委员，无需为每个案件重复锁仓资产。
- 激励机制调整：真正进入仲裁时，由败诉方被罚没的保证金按比例奖励参与裁决的委员，同时可根据协议收入适当补充基础激励，不依赖固定年化收益。
- 该设计降低委员参与成本，奖励与实际贡献直接挂钩，更适配去中心化治理与长期运行的经济模型。

## 5. 术语语义修正（Q13）
**问题描述**：“争议”的语义与实际场景不符
**修复方案**：将术语“争议”统一替换为“分歧”。

## 6. 其他问题归类
### 6.1 代码类问题（Q3、Q7、Q8、Q14、Q15）
属于代码实现层面问题，与机制设计无关，按代码缺陷单独处理。

### 6.2 流程优化类问题（Q9、Q10、Q11）
属于正常测试流程内的优化项，不归类为机制缺陷，划入优化需求池。