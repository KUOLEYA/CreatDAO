# CreatDAO 测试缺陷整理

> 基于完整测试流程的实际运行体验，整理发现的问题。暂不修复，仅记录。

---

## 一、核心体验缺陷

### 1. 争议必然发生，共识几乎不可能

**场景**：§3.3.7 结束讨论，管理员调用 `finalizeDiscussion` 后，合约比较「社区投票胜出方案哈希」与「审计团队报告哈希」。

**问题**：社区报告和审计团队报告使用完全不同的模板和内容结构，哈希必然不同。共识路径（两哈希相等）在正常测试中几乎不可能出现。用户会困惑"为什么方案哈希总是不一致，是不是系统有问题？"

**当前状态**：文档已在 §3.3.7 标注"正常流程，几乎必然"，但这是设计层面的问题，不是文档问题。

**建议方向**：要么让共识变得可能（比如社区可引用审计报告），要么去掉共识路径，争议直接作为唯一路径。

---

### 2. contract.js 版本号分散在 9 个 HTML 文件

**问题**：每次 ABI 修改需同步更新 9 个文件中的 `contract.js?v=N`，容易遗漏。

**涉及文件**：ai-review.html, audit-team.html, community-report-writing.html, pricing.html, proposal-writing.html, dispute-committee.html, admin.html, dashboard.html, report-writing.html

---

## 二、流程引导缺陷

### 3. 争议流程各步骤之间缺少衔接提示

**场景**：§3.3.8 争议 3 步流程中，不同步骤需要不同角色在不同页面操作。

**问题**：
- 步骤① Owner 裁决后进入步骤②，但审计团队（地址2）不会收到任何通知
- 步骤② Owner 裁决后进入步骤③，但委员会（地址5-9）也不会收到通知
- 全靠测试人员手动按文档操作，真实场景下会断链

---

### 4. 委员质押时机不明确

**场景**：§3.3.8 步骤③-0，委员会成员质押。

**问题**：文档要求委员会在步骤③（CommitteeRuling）才质押，但实际上委员会可能永远不被触发（如果步骤②社区认可了修订）。委员的 CEAT 质押在多数测试中白费。

---

## 三、文档缺陷

### 5. §3.3.2 有一个乱码字符

**位置**：第 202 行

```
确认风险评级已选中（� 低危应高亮显示）
```

`�` 应为 `🟢` 或直接删除。

---

## 四、测试操作体验缺陷

### 6. 多账户切换繁琐

**问题**：一次完整测试需要在 10 个 MetaMask 账户间切换约 15-20 次（地址10创建提案 → 地址2提交报告 → 地址3/4/10质押领取提交 → 地址1开启投票 → 地址3/4/10投票 → 地址1结束投票/讨论/裁决 → 地址2修订 → 地址3/4/10争议投票 → 地址5-9质押投票 → 地址3/4/10领奖）。

每次切换需要：打开 MetaMask → 选择账户 → 确认。流程容易被中断。

---

### 7. Hardhat 重启后 MetaMask 需重置

**问题**：文档第 88 行已提到，但每次 Hardhat 重启后，MetaMask 中所有账户的 nonce 和交易历史会与实际链状态不一致，必须逐个账户执行「设置 → 高级 → 重置账户」，10 个账户就是一键 10 次。

---

### 8. 重新部署后必须重启后端

**问题**：文档第 119 行已强调，但容易遗忘。不重启后端会导致 `.env` 中合约地址与 Hardhat 实际部署不一致，链上操作失败。

---

## 五、架构/设计层面的潜在问题

### 9. 委员会几乎永远不会被触发

**问题**：争议 3 步流程中，只要社区在步骤②认可了修订报告，提案直接 Finalized，委员会步骤（③）被跳过。委员会成员（地址5-9）在绝大多数测试中无操作机会、无奖励。

当前设计中，委员会只在「社区拒绝审计团队报告（步骤①）」且「社区拒绝二次复核（步骤②）」时才会被触发。这需要社区连续两次投票拒绝，概率较低。

---

## 六、前端代码缺陷（运行时错误 — 功能直接不可用）

### 10. proposal-writing.html — `isConnected` 变量未定义，提交永远失败

**文件**：`frontend/proposal-writing.html` 第 640 行

**问题**：
```javascript
if (!isConnected)  // isConnected 从未声明，永远是 undefined → !undefined === true
```
即使用户已连接钱包，"提交提案"按钮也永远弹出"请先连接钱包"。

**修复**：改为 `if (!Wallet.isConnected())`。

---

### 11. dispute-committee.html — `handleError` 函数不存在

**文件**：`frontend/dispute-committee.html` 第 343 行

**问题**：`confirmVeto` 的 catch 块调用 `handleError(e, "vetoConfirmResult")`，但 `handleError` 在整个项目中从未定义。出错时抛出 `ReferenceError: handleError is not defined`。

**修复**：改为 `showContractError("vetoConfirmResult", e)`（common.js 已有此函数）。

---

### 12. report-writing.html — 加载原报告时 DOM 元素 ID 错误（3处）

**文件**：`frontend/report-writing.html` 第 895-897 行

**问题**：`loadForRevise` 函数从后端加载原报告后，尝试填充以下字段：

| 代码中使用的ID | 页面实际ID |
|---------------|-----------|
| `riskReason` | `riskExplanation` |
| `coreFindings` | `keyFindings` |
| `finalAdvice` | `finalRecommendations` |

导致 `null.value = ...` 报错，核心结论函无法填充。

---

### 13. report-writing.html — 提交报告时后端字段名错误（数据静默丢失）

**文件**：`frontend/report-writing.html` 第 823-831 行

**问题**：`submitReport` 向 `/reports/save` 发送数据时：

| 发送的字段 | reportData 实际字段 |
|-----------|-------------------|
| `riskReason` | `riskExplanation` |
| `coreFindings` | `keyFindings` |
| `finalAdvice` | `finalRecommendations` |
| `evidence` | 不存在（应分别取 pocCode/traceAnalysis/attackComparison） |

后果：核心结论数据全部为 `undefined`，后端存储不完整报告。

---

### 14. admin.html — `setVoteRewardPenaltyRates` 方法不存在

**文件**：`frontend/admin.html` 第 865 行

**问题**：点击投票奖惩率"设置"按钮时调用 `_A().setVoteRewardPenaltyRates(...)`，但 contract.js ABI 中实际方法为 `setVoteRewardRates(uint256, uint256)`。

**修复**：改为 `setVoteRewardRates`。

---

## 七、合约 ABI 与数据完整性缺陷

### 15. contract.js ABI 缺少 `vetoRequested` / `vetoApproved` 字段

**文件**：`frontend/contract.js` 第 38 行（`getProposalSummary` 的 ABI outputs）

**问题**：ABI 只声明了 10 个返回字段，缺少 `vetoRequested` 和 `vetoApproved`。以下 3 个页面尝试访问这两个字段：
- `audit-team.html`（否决重复检查）
- `dashboard.html`（显示信息）
- `dispute-committee.html`（显示信息）

ethers v5 按 ABI 声明的返回类型解析，未声明字段永远是 `undefined`。

**影响**：audit-team.html 中 `proposeVeto` 的重复否决检查（`if (p.vetoRequested)`）永远为 false，完全失效。

---

### 16. audit-team.html / proposal-writing.html — matrixHash 硬编码为 HashZero

**文件**：
- `frontend/audit-team.html` 第 231 行
- `frontend/proposal-writing.html` 第 660 行

**问题**：两个页面提交报告时 matrixHash 硬编码为 `ethers.constants.HashZero`，而非从页面内容动态计算。链上所有报告的 matrixHash 均为零哈希，共识判定矩阵哈希比较失去意义。

---

### 17. report-writing.html — `s.contractAddress` 在 ABI 中不存在

**文件**：`frontend/report-writing.html` 第 885 行

**问题**：`checkSecondReview` 中 `s.contractAddress` 取不到值（`getProposalSummary` 返回值无此字段），合约地址输入框总是被清空。

---

## 八、UI/UX 细节缺陷

### 18. home.html 缺少钱包连接和网络切换功能

**文件**：`frontend/home.html`

**问题**：home.html 的 header 中只有 logo + 标题，没有钱包连接按钮、网络状态显示（network-pill）、也没有"← 返回主页"链接。与其他所有页面 header 布局严重不一致。用户无法在平台入口页连接钱包。

---

### 19. admin.html — 审计团队地址输入框无 placeholder

**文件**：`frontend/admin.html`（`id="auditTeamAddr"` 的 input）

**问题**：无 placeholder 属性，用户不知道应输入什么格式。

---

## 九、代码质量/维护性缺陷

### 20. 多个页面重复定义 `showPending` / `showSuccess` / `showError`

**文件**：`frontend/report-writing.html`、`frontend/proposal-writing.html`

**问题**：这些页面覆盖了 `common.js` 中的同名全局函数。当前版本功能一致，但若 `common.js` 更新，这些页面不会同步获得更新。应删除页面级定义，统一使用 `common.js` 版本。

---

### 21. dispute-committee.html 手动构造合约实例，绕过统一管理

**文件**：`frontend/dispute-committee.html` 第 339-342、350-351 行

**问题**：`confirmVeto` 和 `viewVetoProposal` 使用 `new ethers.Contract(...)` 手动构造，而非通过 `Wallet.getContracts()`。网络切换时不会自动更新合约实例。

---

### 22. home.html footer 年份写死 2024

**文件**：`frontend/home.html` 第 344 行

**问题**：`<p>CreatDAO (c) 2024 | 审计 DAO 操作平台</p>`，年份硬编码。

---

### 23. 多个页面缺少 `<meta http-equiv="Cache-Control">`

**文件**：`home.html`、`index.html`、`pricing.html`

**问题**：入口页面未设置缓存控制，用户可能看到浏览器缓存的旧版页面。

---

### 24. report-writing.html — `checkSecondReview` 条件判断冗余

**文件**：`frontend/report-writing.html` 第 862 行

**问题**：`if (!Wallet.isConnected || !Wallet.isConnected()) return;` — 第一个是函数引用（永远 truthy），写法令人困惑。去掉即可。

---

### 25. config.js — `selectRisk` 不设置页面级变量

**文件**：`frontend/config.js` 第 232-238 行

**问题**：全局 `selectRisk` 只设置 `window.__selectedRisk`，不设置 `selectedRisk`。依赖此函数的页面须同时检查两个变量。
